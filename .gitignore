# ===== Environments / external libraries (not ours, huge) =====
venv/
third_party/
data/
**/__pycache__/
*.pyc
*.pyo

# ===== Model weights / checkpoints (28+ GB; each file > GitHub 100MB limit) =====
*.safetensors
*.pt
*.pth
*.bin
*.ckpt
*.onnx
**/checkpoints/
**/*_checkpoints/

# ===== HuggingFace / dataset caches (live under ~/.cache anyway) =====
**/hub/
**/.locks/
**/datasets--*/
*.arrow

# ===== misc =====
.DS_Store
*.tmp
*.swp
nohup.out
core.*

# NOTE: videos (*.mp4), plots (*.png), results (*.json/*.csv), logs (*.log),
# code (*.py/*.sh) and docs (*.md) are intentionally TRACKED.

# ===== ACT: LIBERO-Plus robustness results are now TRACKED & pushed (2026-06-27) =====
# Previously git-ignored; now included so the OOD numbers/plots live in the repo.
# Canonical OOD set: ACT/act_results/*/runs/eval_lerobot_full/robustness_plus.json
#
# Abandoned aug experiments + scratch markers stay LOCAL-ONLY here (backed up to
# Hugging Face act-deeponet-libero-checkpoints/extra/ for full recreatability).
ACT/act_results_aug/
ACT/act_results_aug_probe/
ACT/**/CLEAN_PLUS_*
ACT/**/RERUN_DONE
ACT/**/AUG_*
ACT/*.out
